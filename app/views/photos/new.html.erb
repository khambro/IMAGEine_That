<div class="container">



  <%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>
      <ul>
      <% @band.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id, value: current_user.id %><br />
  <div class="field">
    <%= f.label :description %>:
    <%= f.text_field :description %><br />
  </div>
  <div class="field">
    <%= f.label :image_file %>:
    <%= f.file_field :image_file, :required => true %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>



</div>
